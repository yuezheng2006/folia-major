interface:
  display_name: "Folia Major"
  short_description: "Repo-specific patterns and workflows for folia-major"
  default_prompt: "Use the folia-major repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true